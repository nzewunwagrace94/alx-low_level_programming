#include <stdio.h>

int main(void)
{
	/**
	 * the main function is the entry point for C program
	 * it print "Programming is like building a multilingual puzzle
	 *
	 * return 0 for successful execution
	 */
	puts("Programming is like building a multilingual puzzle: \"");
	return (0);
}
