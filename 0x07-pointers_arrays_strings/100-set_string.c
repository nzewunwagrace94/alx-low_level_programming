#include "main.h"

/**
 * set_string - it sets the pointer to a char or string
 * @s: the pointer to pointer
 * @to: pointer to char
 * Return: always 0 as success
 */

void set_string(char **s, char *to)

{
	*s = to;
}
